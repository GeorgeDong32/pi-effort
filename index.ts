import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function effortExtension(_pi: ExtensionAPI): void {
  // Intentional no-op scaffold.
  // The /effort command will be added once the command semantics are finalized.
}
